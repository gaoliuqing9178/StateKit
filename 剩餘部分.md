这份文件给下一个 AI 做直接交接。先读仓库根目录的 `CHANGELOG.md`，再读这里。

## 当前已完成

### 1. 发布面与仓库说明已经补齐

- 根 README、中文 README、包级 README、npm 元数据和根 `CHANGELOG.md` 已经补上。
- 包级可发布内容和 smoke install 流程此前已经补齐。

### 2. CTA Action API 已完成

当前 `StateAction` 已支持：

- `label`
- `href`
- `disabled`
- `loading`
- `loadingLabel`
- `onClick`

同时：

- `primaryAction` / `secondaryAction` 现在支持 `null`
- `undefined` 继续沿用预设默认按钮
- `null` 用于显式移除默认按钮
- 共享 shell 已统一处理按钮 / 链接渲染、重复点击拦截、loading 文案和 disabled / loading 视觉状态

关键文件：

- `H:\StateKit\packages\shared\src\types.ts`
- `H:\StateKit\packages\vue\src\types.ts`
- `H:\StateKit\packages\vue\src\base\StatePresetBlock.vue`
- `H:\StateKit\packages\vue\src\base\StateBlockShell.vue`
- `H:\StateKit\packages\vue\src\styles\base.css`

### 3. Docs 的 Blocks 详情页已经重写

`apps/docs/src/views/BlockDetailView.vue` 已经从简单预览页改成详细用法页，当前覆盖内容包括：

- 组件适用场景
- 默认 layout / density / tone / 默认 CTA
- `title`、`description`、`tone`、`density`、`layout` 的传法
- `primaryAction`、`secondaryAction` 的传法
- 自定义按钮文案
- `onClick` 点击事件
- `href` 链接按钮
- `loading` / `loadingLabel`
- `disabled`
- `null` 移除默认按钮
- 直接传值、脚本变量绑定、`v-bind` 整对象三种传参方式

相关文件：

- `H:\StateKit\apps\docs\src\views\BlockDetailView.vue`
- `H:\StateKit\apps\docs\src\lib\example-code.ts`
- `H:\StateKit\apps\docs\src\styles.css`

### 4. Blocks 页面中宽度布局问题已纳入修正

当前 docs 样式已经补了：

- detail 区域的 `min-width: 0`
- 多档网格折叠
- detail prop / doc / guide 卡片的响应式列数调整

目标是避免在 1700px 到 1120px 这段宽度里出现 detail sections 被遮挡或裁切。

如果后面还要继续微调，优先从这里看：

- `H:\StateKit\apps\docs\src\styles.css`

### 5. Example 页面已按当前功能重写

docs 中的三个 example 页已改成真实交互场景，不再是旧的占位展示：

- `Admin Empty States`
- `Permissions And Upgrade`
- `Task Flow`

外部示例也已同步：

- `H:\StateKit\examples\vite-vue-admin\src\App.vue`
- `H:\StateKit\examples\vite-vue-admin\src\styles.css`

这些 example 现在明确展示：

- `onClick`
- `href`
- `loading`
- `loadingLabel`
- `disabled`
- `null` 移除次要按钮

### 6. 文档说明已同步更新方向

如果继续改对外说明，优先同步这几类文件：

- 根 `README.md`
- 根 `README.zh-CN.md`
- 根 `CHANGELOG.md`
- `docs/statekit-block-spec.md`
- `docs/statekit-visual-system.md`
- `docs/statekit-docs-site-and-qa-spec.md`

## 当前已验证

最近一次在 example 重写后重新跑过：

```bash
npm run typecheck
npm run build
```

两项都已通过。

补充说明：

- `pack:check` 和 `smoke:install` 之前在包发布与 CTA API 调整阶段跑通过。
- 但它们没有在这轮 docs / example / README 草稿整理之后重新跑，因为这轮新增变更主要集中在文档和示例展示。

## 当前剩余 / 下一步建议

当前用户明确提出的这批需求，代码层面没有硬阻塞项了。

如果下一轮还要继续做，建议按这个顺序：

1. 用浏览器人工检查 docs 的 block 详情页和 example 页。
2. 重点看这些宽度：1700、1440、1280、1120、760。
3. 如果后续还会继续改包内容或准备发布，再补跑：

```bash
npm run pack:check
npm run smoke:install
```

## 额外提醒

- 仓库现在本来就是 dirty worktree，不要随手回退已有改动。
- 如果再改 block 用法文档，优先同步 `BlockDetailView.vue` 和 `example-code.ts`，否则文案很容易再次和真实 API 脱节。