/**
 * StateKit ?????
 * 1. Vue ??? props ?????
 * 2. ??????? block ?????????????????? CTA ???
 * 3. ????????????? shared ???????????????
 */

import type { StateAction, StateDensity, StateLayout, StateTone } from "@statekit/shared";
export interface PresetStateBlockProps { title?: string; description?: string; tone?: StateTone; density?: StateDensity; layout?: StateLayout; primaryAction?: StateAction; secondaryAction?: StateAction; }
